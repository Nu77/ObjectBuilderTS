export class Size {
    public width: number;
    public height: number;

    constructor(width: number = NaN, height: number = NaN) {
        this.width = width;
        this.height = height;
    }
}

